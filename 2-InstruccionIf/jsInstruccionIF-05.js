// Entregado

function mostrar()
{
	var edadAdolescente;

	edadAdolescente = parseInt(document.getElementById("txtIdEdad").value);

	if(edadAdolescente < 12 || edadAdolescente > 17)  

	alert("No es adolescente");

	
}//FIN DE LA FUNCIÓN